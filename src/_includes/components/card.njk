{% macro card(title, subtitle = '', icon = '', url = '', tags = '') %}
  <div class="card padding-xlarge round shadow">
    <div class="card-container">
      <div class="center">
        <a href="{{ url }}">
          <img src="/img/icons/{{ icon }}.svg" />
        </a>
      </div>
      <div class="card-details">
        <a href="{{ url }}">
          <h2 class="margin-none capitalize">{{ title }}</h2>
        </a>
        {% if tags -%} {%- include "_tags.html" -%} {%- endif %} {% if subtitle
        -%}
        <p class="margin-none max-lines-3">{{ subtitle }}</p>
        {%- endif %}
      </div>
      {%- button "", url, "center margin-none" -%}
    </div>
  </div>
{% endmacro %}
