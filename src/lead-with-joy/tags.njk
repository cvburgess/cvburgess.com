---
layout: base.njk
pagination:
  data: collections
  size: 1
  alias: tag
permalink: /tags/{{ tag | slugify }}/
---
{% from 'components/card.njk' import card %}

<h1>Category is: &nbsp;<strong>{{ tag | upper }}</strong>
</h1>
<div class="cards stack">
  {%- for post in collections[ tag ] | reverse -%}
    {{ card(title = post.data.title, subtitle = post.data.subtitle, icon = post.data.type, url = post.data.url) }}
  {%- endfor -%}
</div>

<div class="center margin-xlarge-vertical">
  <a href="/lead-with-joy">← Back to the Hub</a>
</div>